[project]
name = "kv-store-workspace"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
    "services/storage",
    "services/proxy",
]