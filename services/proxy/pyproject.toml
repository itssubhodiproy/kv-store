[project]
name = "kv-store-proxy"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "grpcio>=1.83.1",
    "grpcio-tools>=1.83.1",
    "httpx>=0.28.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
proxy-node = "kv_store_proxy.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kv_store_proxy"]
